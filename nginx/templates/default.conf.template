server {
	listen ${NGINX_PORT};
	server_name ${NGINX_SERVER_NAME};

	location / {
		proxy_set_header X-Forwarded-Proto $scheme;
		proxy_set_header X-Forwarded-Host $host;

		proxy_pass http://skrambl:${DENO_PORT};
	}

	location /static/ {
		root /usr/share/nginx/;
	}
}
